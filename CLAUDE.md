# 『永い夢』創作資料集 ── 編集規約

**この資料集に何かを書き足す・書き換えるときは、以下を必ず守ること。** ユーザーが資料を追加していくたびに、この規約に沿って統合する。

---

## 1. 大原則:圧縮 ≠ 削除

**情報をそぎ落とすのではなく、密度を上げる。** 読み手が同じイメージを復元できることが絶対条件。

- **消していいのは「冗長」だけ** ── 重複、言い換えの繰り返し、見出しの再述、間投的な接続語
- **消してはいけないもの** ── 台詞、固有名詞、数値、能力仕様、`※` の解釈・演出解説
- 迷ったら残す。**削るより言い換えて詰めるほうを常に優先する**

**特に台詞(`>` 引用)は一字も変えない。** 作品の体温はここに宿っている。

## 2. Single Source of Truth ── 事実は1箇所にしか書かない

同じ事実を2つのファイルに書いてはいけない。**必ず「持ち主」を決め、他方からはリンクする。**

| 種類 | 持ち主 |
|---|---|
| 世界の共通ルール(能力とは何か、社会、戦闘、読む上での注意) | `README.md` |
| 人物の静的プロフィール(出自・容姿・能力仕様・人物像・最期) | `INDEX.md` |
| 出来事・戦闘の推移・その編固有の解釈 | `arc1`〜`arc10` |
| 話数と「第◯話に何が起きたか」 | `TIMELINE.md` |
| 組織概要・場所・アーティファクト | その組織が主役の arc |

**書き足すときの手順:**

1. その情報の「持ち主」がどのファイルか決める
2. **他のファイルに同じ記述がすでにないか確認する**(あれば片方をリンクに置き換える)
3. 参照が必要な場所には `→ [INDEX](INDEX.md#見出し)` のようにリンクだけ置く

※ 過去に arc1 が INDEX のイタチ・カタストロフィ項をほぼ丸ごと複製していた、といった事故が実際にあった。**同じ文章が2箇所にあると、片方だけ更新されて矛盾する。**

## 3. アンカーリンクを壊さない

**見出しの文字列は他ファイルからリンクされている。** 既存の見出しは、圧縮のためであっても文言を変えない。

- アンカーの生成規則(GitHub互換):小文字化 → 文字・数字以外の記号(`『』「」、。?:──` など)を除去 → **半角スペースを1つずつ `-` に置換**(連続スペースは連続ハイフンになる)
- 例:`## 幕間 ── アンデルセン、連行される` → `#幕間--アンデルセン連行される`(`──` が消えて両側の空白が `--` になる)
- 見出しを変えざるを得ないときは、**全ファイルの参照リンクを同時に直す**

## 4. 記法

- `※` = 注釈・演出解説・解釈。それ以外の地の文は作中の事実
- **太字** = 真相・オチ・決定的な部分
- 未確定事項は各ファイル末尾の `# 未確定` にまとめる(推測で埋めない)
- 新情報の一時置き場は `UNSORTED.md`。**該当ファイルへ移したら、その項だけ UNSORTED から削除する**

## 5. 世界観に関する頻出の落とし穴

- **[編は世界の分割ではなく、切り取り方の分類](README.md#編は世界の分割ではなく切り取り方の分類)。** 編の話数範囲と、そこに出る組織・人物の活動範囲は**まったく別物**。「arc5の話数範囲外にその組織が出てくるのはおかしい」といった存在しない矛盾を作らないこと
- **能力に共通仕様は存在しない。** 能力Aの性質から能力Bを推論しない
- **敗北は弱さの証明ではない。** 勝敗だけで人物の格を書き換えない

## 6. 除外

`初めてもらった素晴らしい感想.md` は**読者から受け取った文章そのもの**。**圧縮・要約・改稿の対象外**、一字も変えない。
(ここにしかない情報を資料側で使いたい場合は、引用ではなく事実として該当ファイルへ書く。)
